package by.training.model;

/**
 * The enum Role represent the text designation for a {@link User} role.
 */
public enum RoleEnum {
    /**
     * Admin role enum.
     */
    ADMIN,
    /**
     * Moder role enum.
     */
    MODER,
    /**
     * User role enum.
     */
    USER
}
